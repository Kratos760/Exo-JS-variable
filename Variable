const a = 2;
const b = 2;
const c = 3;
if (a === b && b === c && c === a){
  console.log("three are same");
} else if (a === b || b === c || c === a){
  console.log("two are same");
} else {
  console.log("All aren't same");
}
